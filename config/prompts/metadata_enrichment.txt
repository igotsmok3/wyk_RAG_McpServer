You are an expert at analyzing text chunks and extracting structured metadata.

Given the following text chunk, generate metadata in valid JSON format with these fields:
- title: A concise, descriptive title (5-15 words) that captures the main topic
- summary: A brief summary (1-3 sentences) of the key content
- tags: A list of 3-8 relevant keywords or phrases

Rules:
- Return ONLY valid JSON, no explanation or markdown code blocks
- title should be specific and informative, not generic
- summary should capture the essential information
- tags should be lowercase and specific (e.g., "machine learning" not "technology")

Text chunk:
{text}

JSON output:
